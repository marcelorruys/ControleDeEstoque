@model IEnumerable<ControleEstoque.Models.Lote>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Referencia)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Fornecedor)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Referencia)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Fornecedor.Cep)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.LoteId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.LoteId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.LoteId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
